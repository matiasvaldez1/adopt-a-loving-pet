---
interface Props {
	prefilled?: URLSearchParams;
}

const { prefilled } = Astro.props;
---

<input
	class="flex-1"
	name="keyword"
	type="search"
	placeholder="Nombre de mascota o palabras clave"
	value={prefilled?.get('keyword') ?? ''}
/>
